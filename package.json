{
  "name": "jk-connect",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "postinstall": "cd backend && npm install && npx prisma generate && npx prisma db push && cd ../frontend && npm install && npm run build",
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build"
  }
}
